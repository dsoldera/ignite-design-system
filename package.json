{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "check-types": "tsc --noEmit"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  },
  "packageManager": "npm@8.5.0"
}
